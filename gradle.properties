org.gradle.jvmargs=-Xmx10g
android.enableJetifier=false
android.useAndroidX=true
org.gradle.configuration-cache=true
# R8 needs ~4 GB per app module; run one R8 task at a time so all demo modules can be minified in one build.
android.r8.maxWorkers=1
